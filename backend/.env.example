# Base de datos MongoDB
MONGO_URI=mongodb+srv://usuario:contraseña@cluster.mongodb.net/nombreDB

# Entorno de ejecución (development, production, test)
NODE_ENV=development

# Puerto en el que corre el backend
APP_PORT=3001

# Nivel de logs
LOG_LEVEL=debug

# Secreto para firmar JWT (cambiar por una cadena segura)
JWT_SECRET=supersecreto123

# URL del nodo RPC de la blockchain (ej. Infura, Alchemy, o tu propio nodo)
RPC_URL=https://polygon-rpc.com

# Clave privada del wallet (¡NUNCA uses tu clave real en este archivo!)
PRIVATE_KEY=0xabc123...tuPrivateKey

# Clave privada del wallet de distribución de tokens
PRIVATE_DISTRIBUTION_KEY=0xabc123...tuPrivateDistributionKey

# Dirección del contrato del token ERC-20 (o BEP-20, etc.)
TOKEN_ADDRESS=0x123abc456def789...tokenContractAddress

# Dirección del wallet que distribuirá los tokens
DISTRIBUTION_WALLET_ADDRESS=0xabc456def123...walletAddress
